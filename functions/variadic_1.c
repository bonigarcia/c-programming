#include <stdio.h>

int main() {
    int number = 10;
    char character = 'a';

    printf("Using printf\n");
    printf("Number is %d\n", number);
    printf("Number is %d and character is %c\n", number, character);

    return 0;
}

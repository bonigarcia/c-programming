#include <stdio.h>

int main() {
    char character1 = 'a';
    char character2 = 'a';

    if (character1 == character2) {
        printf("'%c' and '%c' are EQUAL\n", character1, character2);
    }

    return 0;
}
